import React from "react";
import {string} from "prop-types";

const Tile = ({props})=> <div>

</div>

Tile.propTypes = {
    image: string,
    title: string,
    review: string
}

export default Tile
